export interface TabContent {
  title: string;
  subtitle: string;
  description: string;
}

export interface Account {
  name: string;
  signal: string;
}

export interface ChangelogItem {
  date: string;
  title: string;
  desc: string;
}
